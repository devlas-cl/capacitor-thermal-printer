<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- USB host (OTG) — required=false: el plugin también sirve por TCP/BT en equipos sin OTG -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- TCP/LAN/WiFi (raw 9100) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Descubrimiento de impresoras de red: mDNS (NSD) + leer la IP/subred WiFi
         para el barrido de la /24 local. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Modo host de impresión: anunciarse y descubrir hosts por mDNS necesita
         mantener vivo el multicast en algunos equipos/routers. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Bluetooth SPP: legacy hasta API 30, BLUETOOTH_CONNECT runtime desde API 31 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

</manifest>
